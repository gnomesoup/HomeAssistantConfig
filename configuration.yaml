homeassistant:
  # Name of the location where Home Assistant is running
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: !secret latitude
  longitude: !secret longitude
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: !secret elevation
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: America/Chicago
  # Customization file
  customize: !include customize.yaml

# Show links to resources in log and frontend
# introduction:

# Enables the frontend
frontend:

# Enables configuration UI
config:

# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
http:
  base_url: !secret http_url
  ssl_certificate: !secret http_certificate
  ssl_key: !secret http_key

# Checks for available updates
# Note: This component will send some information about your system to
# the developers to assist with development of Home Assistant.
# For more information, please see:
# https://home-assistant.io/blog/2016/10/25/explaining-the-updater/
updater:
  # Optional, allows Home Assistant developers to focus on popular components.
  # include_used_components: true

# Discover some devices automatically
discovery:

# Allows you to issue voice commands from the frontend in enabled browsers
conversation:

# Enables support for tracking state changes over time
history:

# View all events in a logbook
logbook:

# Enables a map showing the location of tracked devices
map:

device_tracker:
  - platform: icloud
    username: !secret icloud_1
    password: !secret icloud_1_password
    account_name: !secret icloud_1_descriptive

# Track the sun
sun:


# Text to speech
tts:
  - platform: google

# Cloud
cloud:

# MQTT
mqtt:
  broker: 10.0.0.120

    
switch: !include switch.yaml
sensor: !include sensor.yaml
group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml

# Smartthings MQTT json state

# smartthings/Bedroom Canopy/energy": "125.472",
# smartthings/Bedroom Canopy/power": "0",
# smartthings/Bedroom Thermostat/coolingSetpoint": "74",
# smartthings/Bedroom Thermostat/temperature": "74",
# smartthings/Bedroom Thermostat/thermostatOperatingState": "fan only",
# smartthings/Bedroom Thermostat/thermostatSetpoint": "74",
# smartthings/Livingroom Thermostat/coolingSetpoint": "75",
# smartthings/Livingroom Thermostat/temperature": "75",
# smartthings/Livingroom Thermostat/thermostatOperatingState": "fan only",
# smartthings/Livingroom Thermostat/thermostatSetpoint": "75",
# smartthings/Metal Lamp/energy": "31.014",
# smartthings/Metal Lamp/power": "0",
# smartthings/Minimote Bedroom button 1/button": "pushed",
# smartthings/Minimote Bedroom button 2/button": "pushed",
# smartthings/Minimote Bedroom/button": "pushed",
# smartthings/Minimote Door button 2/button": "pushed",
# smartthings/Minimote Door/button": "pushed"
# smartthings/Patio Camera/motion": "inactive",
# smartthings/Patio Camera/sound": "not detected",
# smartthings/Patio Camera/switch": "on",
# smartthings/Pop Socket Light/energy": "4.730",
# smartthings/Pop Socket Light/power": "0",
# smartthings/Water Leak Sensor/temperature": "73",
